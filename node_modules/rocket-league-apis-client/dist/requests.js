'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPlayerInformation = undefined;

var _isomorphicFetch = require('isomorphic-fetch');

var _isomorphicFetch2 = _interopRequireDefault(_isomorphicFetch);

var _querystring = require('querystring');

var _querystring2 = _interopRequireDefault(_querystring);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var getPlayerInformation = exports.getPlayerInformation = function getPlayerInformation(tracker, apiUrl, apiKey, platform, id) {
  var request = tracker.createRequest(apiUrl, apiKey, platform, id);
  var url = request.url + '?' + _querystring2.default.stringify(request.query);
  var options = {};
  if (request.headers) options.headers = request.headers;
  return new Promise(function (resolve, reject) {
    (0, _isomorphicFetch2.default)(url, options).then(function (res) {
      tracker.handleResponse(res).then(function (formattedRes) {
        resolve(tracker.formatPlayerResponse(formattedRes));
      }).catch(function (err) {
        return reject(err);
      });
    }).catch(function (err) {
      return reject({ data: err, message: 'ServerError' });
    });
  });
};